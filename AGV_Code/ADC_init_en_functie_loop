void init_adc(void)
{
    ADMUX |= (1 << REFS0);
    ADCSRA = (1 << ADPS2) | (1 << ADPS1) | (1 << ADPS0);
    ADCSRA |= (1 << ADEN);
}

void ADC_Check(void)
{
    ADCSRA |= (1 << ADSC);
    ADC_waarde = ADC;
}
